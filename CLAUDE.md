# أتمتة الزيارات الإشرافية — سياق المشروع

## ما هو المشروع

موقع `supervisor-mct.com` يُنشئ تقارير الزيارات الإشرافية والمدرسية، وسكربت
تامبر مانكي ينقل التقرير إلى بوّابة وزارة التربية العمانية ويعبّئ الاستمارة
تلقائياً. المستخدم مشرف مادة في دائرة الإشراف التربوي.

## الملفات

| الملف | المكان | الدور |
|---|---|---|
| `school-visits-automation.user.js` | جذر الموقع | سكربت تامبر مانكي (v15.3) — يُثبَّت في المتصفّح ويُستضاف للتحديث التلقائي عبر `updateURL` |
| `selectors.json` | جذر الموقع | خريطة معرّفات البوّابة — تُجلب كلّ ٦ ساعات، تعديلها لا يحتاج إصدار نسخة جديدة |
| `js/queue-export.js` | الموقع | تحديد زيارات الأرشيف ورفعها طابوراً واحداً إلى البوّابة |
| `js/export.js` | الموقع | التصدير المفرد القائم (`exportToMoe`, `exportSchoolVisitToMoe`) |
| `tests/queue-and-config.test.js` | المستودع | اختبار انحدار: `node tests/queue-and-config.test.js` — يشغّل الشيفرة الحقيقيّة من الملفّات |
| `js/identity.js` | الموقع | هوية المشرف والربط الاختياريّ بقاعدة بيانات المعلمين — اختباره `node tests/identity.test.js` |
| `js/autofill.js` | الموقع | الإكمال التلقائيّ للنماذج من القائمة المرتبطة (يُحمَّل بعد `school.js` وقبل `init.js`) |
| `js/cloud.js` | الموقع | النسخة السحابيّة للتقارير — اختباره `node tests/cloud.test.js` |
| `monthly.html` + `js/monthly-core.js` + `js/monthly-docx.js` + `js/monthly.js` | الموقع | التقرير الشهري وحزمته — اختباره `node tests/monthly.test.js` |
| `js/recs.js` + `js/recs-ui.js` | الموقع | منشئ توصيات الزيارة المدرسيّة — اختباره `node tests/recs.test.js` |
| `js/classroom.js` | الموقع | المواقف الصفّيّة: القوائم من قاعدة المعلمين، والتقرير الإشرافيّ منها — اختباره `node tests/classroom.test.js` |
| `js/route.js` | الموقع | خطّ سير اليوم «#قادم من مدرسة … / #متجه إلى مدرسة …» أسفل الأهداف — دالّةٌ نقيّة بلا DOM، اختبارها `node tests/route.test.js` |
| `index.html` | جذر الموقع | بوّابة الدخول — مشهد الطابور الصباحيّ وروابط الأنظمة الثلاثة. صفحةٌ واحدة مستقلّة، لا تشارك `styles.css` ولا `js/` |
| `reports.html` | جذر الموقع | **نظام التقارير نفسه** — كان `index.html` حتّى ٢٠ سبتمبر ٢٠٢٦. أيّ رابطٍ داخليٍّ إليه يُكتب `reports.html` |
| `site-shell.html` | المستودع (مصدر) | **قشرة الموقع**: شريط الهويّة وفيه التنقّل بين الأنظمة الأربعة، ومشهد الطابور خلفيّةً، والزجاج، وقواعد التحميل المسبق. تُنسخ حرفيّاً إلى أوّل `<body>` في `reports.html` و`monthly.html` و`plan-mct/index.html` و`teacher-data-collection/index.html` و`data.html`، وتُستبدل بين علامتَي `<!-- قشرة الموقع` و`<!-- /svf-shell -->`. لا تُربط من الشبكة (نطاقاتٌ مختلفة، ونظام التقارير يعمل دون إنترنت)، وتختفي عند الطباعة |
| `sim-moe/SupervisionVisitsModule.html` | **غير موجود بعد** في المستودع ولا الموقع | محاكاة البوّابة للاختبار المحلّي |

**بنية التقرير المحفوظ في الموقع** (`savePermanentReport` في `js/supervisory.js`): المفتاح
`supervision_v6_visit_<ts>`، والجذر فيه `teacherName` و`visitDate` و`school` فقط؛ بقيّة
الحقول داخل `formData` بمعرّفات عناصر النموذج: `topic` (عنوان الدرس)، `lesson` (الحصّة)،
`subject`، `class`، `strengthsContent`، `developmentContent`، `recommendationsContent`،
`score-1..13`، `notes-1..13`. أيّ قارئٍ للأرشيف يقرأ من `formData`.

## توحيد الواجهة بين الصفحات الستّ (منذ 2026-09-21)

الأنظمة على ثلاثة نطاقات (`supervisor-mct.com` و`plan.` و`teachers.`) لكنّها تُقرأ موقعاً واحداً:

- **`data-svf-page` على `<body>` كلّ صفحة** (`reports|monthly|plan|data|form`) يعلّم رابطها في
  شريط الهويّة. و`form` (استمارة المعلّمين) **تُخفي التنقّل كلّه**: يفتحها المعلّم لا المشرف.
- **`@view-transition` في `<head>` لا في القشرة.** الصفحة المستقبِلة تقرؤه قبل أوّل رسم، و`<style>`
  داخل `body` يصل بعده فلا يُفعَّل التلاشي — قِيس: بلا نقلٍ إلى الرأس لم يعمل بين التقارير والشهري.
  والتلاشي **لا يعمل عبر النطاقات** (`plan.` و`teachers.` أصلٌ آخر) — ذلك حدّ المتصفّح.
- **لا يُسمّى عنصرٌ بـ `view-transition-name`**: الاسم يرفعه فوق لقطة الصفحة فيغطّي المحتوى.
  الشريط والمشهد متطابقان في الصفحتين فلا يُرى تغيّرهما بالتلاشي العامّ.
- **التحميل المسبق** (`speculationrules`): ما في الأصل نفسه `prerender` عند المرور، وما في نطاقٍ
  فرعيٍّ `prefetch`. والبوّابة تفتح روابطها **في التبويب نفسه** (كانت `_blank`).
- **الخطّ:** الشاشة `IBM Plex Sans Arabic` في الصفحات كلّها. **الطباعة و PDF يبقيان `Tajawal`** في
  التقارير والشهري والخطة — وسطر `@media print{body{font-family:'Tajawal'!important}}` في رأس كلٍّ
  منها، مع تحميلٍ استباقيٍّ عند `DOMContentLoaded` (الخطّ الذي لا يُستعمل على الشاشة لا يُجلب،
  فتخرج الطباعة ببديلٍ). PDF خطة السير يحدّد `Tajawal` صراحةً في `plan-mct/index.html`.
- **عبارة حقوق المصمّم** (منذ 2026-09-21): «إعداد وتصميم: أ. أسعد الخصيبي / مشرف الرياضة المدرسية /
  جميع حقوق التصميم والتطوير محفوظة © 2026» — الصيغة صيغة المشرف نفسها كما في `AppFooter` بخطة
  السير. مصدرها القسم الثاني في `site-shell.html` بين `<!-- svf-credit -->` و`<!-- /svf-credit -->`،
  وتُنسخ **قبل آخر `</body>`** (لا في أوّله) إلى التقارير والشهري وصفحتَي المعلّمين، وفي البوّابة
  داخل شريطها السفليّ. **خطة السير استثناء**: لها `AppFooter` يظهر في شاشاتها الثلاث فأُضيفت إليه
  جملة الحقوق ولا يُنسخ إليها تذييل القشرة (وإلّا ظهر تذييلان). و`<meta name="author">` و`copyright`
  في رأس الصفحات الستّ. **تُخفى عند الطباعة** لأنّ تقارير الزيارات أوراقٌ رسميّة؛ أمّا نسخة خطة
  السير المطبوعة/PDF فتحمل عبارتها القديمة «إعداد وتصميم: …» من الأصل (`plan-mct/index.html`).
  وتذييل شاشة التقارير الرئيسيّة يبقى فيه سطر الوزارة وحده؛ سطر المصمّم القديم حُذف لأنّ التذييل الجديد يغطّي شاشاتها كلّها.
- **عنوان الصفحة** «الصفحة — منظومة الإشراف التربوي»، و`theme-color` `#0a1410` و`manifest`
  و`favicon` واحدٌ في الستّ.

## كيف تنتقل البيانات

الموقع → البوّابة عبر ثلاث قنوات، الفعّالة منها **الأولى فقط**:

1. **hash في الرابط** — `#svfs=<base64>` للإشرافية، `#svf=` للمدرسية. هذه القناة العاملة.
2. `localStorage` — لا يعمل: النطاقان مختلفان.
3. الحافظة — احتياطيّ.

`GM_setValue` من شيفرة الصفحة **لا يعمل** — تُستدعى من السكربت لا من الصفحة.

## آليّات البوّابة المكتشفة (مهمّة — كلّها كلّفت دورات تصحيح)

- **النموذج يُفتح داخل `iframe`**، لا في صفحة مستقلّة. المستند الرئيسيّ يبقى
  صفحة القائمة بزرّها `ctl00_content_ImgAdd`، فأيّ كشفٍ يعتمد على غياب هذا
  الزرّ يدور في حلقة. الكشف الصحيح: وجود حقول النموذج داخل الإطارات، مع
  انتظارٍ حتّى ٢٥ ثانية لتحميله.

- **اختيار الموظّف ليس `__doPostBack`.** خليّة صفّ النتائج تحوي
  `<div onclick="DisplayInfo('الاسم','الوظيفة','المدرسة','16203690',…)">`.
  النقر على `TR` أو على الخليّة لا يفعل شيئاً — يجب النقر على الـ `div` نفسه.

- **قائمة `ddlForms` تبقى فارغةً حتّى يُثبَّت الموظّف**، وبنود التقييم لا توجد
  في DOM قبل الضغط على `btnAdd`. الضغط قبل اختيار الاستمارة يُنتج:
  «من فضلك اختر استمارة اشرافية».

- **قسم «بيانات زيارة المعلم» مطويّ** بمفتاح توسيعٍ في صفّه (لا تبويب)، وحقل
  عنوان الدرس تحته. البوّابة ترفض بـ«يجب ادخال عنوان الدرس».

- **ترتيب الخطوات إلزاميّ:** فتح النموذج ← البحث ← اختيار المعلّم ← التاريخ ←
  الاستمارة ← فتح بيانات المعلّم ← عنوان الدرس والحصّة والمادّة ← `btnAdd` ←
  بنود التقييم ← حقول الاستمارة ← الحفظ. كلّ postback يمسح ما قبله إن اختلّ الترتيب.

- **البوّابة (ASP.NET Framework 4.0) ترفض الحفظ كاملةً — لا حقلاً واحداً — إن
  حمل أيّ حقلٍ نصّيّ النمط `&#رقم؛` أو `<` متبوعةً بحرفٍ/`!`/`/`/`؟`**
  (`CrossSiteScriptingValidation`، رسالتها `HttpRequestValidationException`).
  وقع هذا فعلاً (2026-09-26): «رأي الزائر» حمل ترميز HTML حرفيّاً (`&#1644;`)
  — على الأرجح من لوحة مفاتيح/إدخالٍ صوتيٍّ أدرج رمزاً غير معتاد — فسقط حفظ
  الزيارة كاملةً برسالةٍ من خادم الوزارة لا من السكربت، ولم يظهر أثرٌ لذلك في
  الموقع نفسه. **`sanitizeForPortal()`** (دالّةٌ مشتركةٌ في السكربت، أعلاه قرب
  `esc()`) تفكّ أيّ ترميز HTML رقميٍّ أو مسمّىً إلى حرفه الحقيقيّ (فلا يضيع
  المعنى) ثمّ تُحيِّد `<` الخطرة وحدها ببديلٍ بصريٍّ آمن (`‹`)، وتمرّ منها **كلّ**
  كتابةٍ في حقول البوّابة عبر نقطتين فقط: `setFieldValue` (الزيارات المدرسيّة)
  و`setVal` (الزيارات الإشرافية) — أيّ نقطة كتابةٍ ثالثة مستقبلاً يجب أن تمرّ
  بها أيضاً. اختباره `node tests/sanitize-portal.test.js`.

## المعرّفات الحقيقيّة (من تشخيص البوّابة)

```
التاريخ        ctl00_applicationPageContentPlaceHolder_visitDataPicker_dateTextBox
بحث الموظّف    ..._EmployeeAdministrativeScaleSearchCtrl1_txtSearchText_AutoCompletTextBox
زرّ البحث      ..._EmployeeAdministrativeScaleSearchCtrl1_btnSearch          (input[type=image])
شبكة النتائج   ..._EmployeeAdministrativeScaleSearchCtrl1_GridviewEmployeeSearchResult
اسم المختار    ..._EmployeeAdministrativeScaleSearchCtrl1_lblEmployeeName    (span)
الاستمارة      ctl00_applicationPageContentPlaceHolder_ddlForms
عنوان الدرس    ..._TeacherManualSchedule1_txtLessonTitle
الحصّة         ..._TeacherManualSchedule1_ddlSessionIndex
المادّة         ctl00_applicationPageContentPlaceHolder_ddlSubjects
إضافة          ctl00_applicationPageContentPlaceHolder_btnAdd                (input[type=image])
بنود التقييم   ..._rptrFormItems_ctl01.._ctl13_ddlItemEvals        (جدول ItemsTable)
حقول نصّيّة     ..._rptrFormFields_ctl01.._ctl04_txtFieldValue      (جدول FieldsTable)
مهارات تخصّصيّة ..._sklsFormSkills_gvProfessionalSkills_ctlNN_ddlSkillPoints / _txtNotes
التبويبات      TM1 (بنود) / TM2 (حقول) / TM3 (مهارات)
```

الالتقاط **بالترتيب لا بالأرقام**: `select[id*="ddlItemEvals"]` ثمّ توزيع
التقييمات الـ13 بالتتابع. ترقيم المكرّر قد يزيح.

## مقابلة الحقول

| الموقع | البوّابة |
|---|---|
| `excellence` | جوانب الإجادة |
| `development` | الجوانب التي تحتاج إلى تطوير |
| `recommendations` | **الدعم المقدم** (هما نفس الشيء عند المستخدم) |
| أيّ خانة فارغة | تُكتب فيها **«لا يوجد»** |

## هوية المشرف والربط بقاعدة بيانات المعلمين (`js/identity.js`)

الموقع يستخدمه ١٥ مشرفاً، كلٌّ على متصفّحه، فالتقارير منفصلةٌ بالجهاز. الربط **اختياريّ**:
بطاقةٌ في الصفحة الرئيسيّة يُدخل فيها المشرف **رمزه في موقع بيانات المعلمين**
(`teachers.supervisor-mct.com`، مشروع Firebase `teacher-data-collection-716bb`). بلا رمزٍ لا
تُحمَّل Firebase ويعمل الموقع كما كان.

- **القائمة** (المعرّف، الاسم، بصمة الرمز، `ADMIN_HASH`) بين علامتَي `SUPERVISORS` يكتبها
  `teacher-data-tools/sync_supervisors.py` من `supervisors.txt` مع صفحتَي موقع المعلمين —
  **لا تُعدَّل يدوياً**. الاختبار يطابقها بـ`data.html` إن وُجد المستودع بجانبه.
- **الحكم نفسه في data.html**: بصمة الرمز تُطابَق بعد الرموز التي غيّرها المشرفون
  (`supervisor_codes`)، ثمّ رمز المشرف العام (يسحب الجميع).
- **السحب مُصفّى في القاعدة** `where('المشرف','==',الاسم)` لا في المتصفّح — وهو شكل الطلب
  الذي تقبله القواعد حين تُقفل القراءة (Firestore ترفض الطلب ولا تُصفّيه).
- **المحفوظ**: `svf_identity` = `{id, hash, linkedAt}` (البصمة لا الرمز)، و
  `svf_teachers_cache_<id>` بالحقول اللازمة فقط — **لا رقم مدنيّ ولا هاتف ولا بريد**. الربط
  بمشرفٍ آخر يمسح نسخ غيره، والنسخة التي لا تحمل معرّف صاحب الهويّة تُهمَل.
- **التحديث** مرّةً يومياً عند الفتح أو بزرّ «تحديث»، ومعه يُتحقَّق من الرمز: تغيّر ← يُلغى
  الربط ويُطلب الجديد. تعذُّر الاتّصال لا يُلغيه.
- **`CACHE_VERSION` يُرفع كلّما أُضيف حقلٌ إلى `FIELDS` أو تغيّر اشتقاق.** نسخةٌ سُحبت بالبنية
  القديمة تبقى «حديثةَ الوقت» أقلّ من يوم فلا تُحدَّث، فتُعبَّأ النماذج ناقصةً **بلا خطأ
  ظاهر**: هكذا غاب رقم الملف عمّن ربط قبل إضافته، والمدرسة تُملأ لأنّها حقلٌ قديم. رفعُ الرقم
  يُجبر السحب عند أوّل فتح (٢٫٥ ثانية في القياس الحيّ).
- مكتبة **Firestore Lite** (11.6.1) بـ`import()` عند الحاجة؛ عامل الخدمة لا يخزّن طلبات
  `firestore.googleapis.com`.
- **تنظيمٌ لا قفل**: قواعد قاعدة المعلمين تسمح بالقراءة لأيّ أحد حتّى المرحلة ٢ (المصادقة).

**ما يُشتقّ عند السحب** (لا يُنسخ خاماً): **الجنس** من «الحالة الاجتماعية» (متزوجة/عزباء/منفصلة ← أنثى)
— القاعدة بلا حقل جنس، والحالة نفسها لا تُحفظ في المتصفّح. و**الصفوف** من مفتاحين: «الصفوف
التي يدرسها» (الخاصّ) أو «الفصول التي تدرسها» (الحكوميّ)، وتُحوَّل إلى نطاق: «9،10،11،12» ←
«9-12»، والمتقطّع يبقى مفصولاً. ويُنقل كذلك **رقم الملف** و**اسم مدير المدرسة**.

## النسخة السحابيّة للتقارير (`js/cloud.js`)

تعمل **بالربط وحده**، كالإكمال التلقائيّ. من لم يُدخل رمزه تبقى تقاريره على جهازه كما
كانت: لا تُحمَّل مكتبةٌ ولا يُرسَل شيء، والبطاقة في الصفحة الرئيسيّة تقول ذلك صراحةً.

- **المسار** `reports/<معرّف المشرف>/items/<مفتاح التقرير>` في مشروع المعلمين نفسه
  (`teacher-data-collection-716bb`). المعرّف (`asad`، `hind`، …) **لا المجموع المُعمّى**: هو
  ثابتٌ لا يتبدّل حين يغيّر المشرف رمزه، فلا يفقد أرشيفه. و«المشرف العام» مسارهُ `admin`.
- **الوثيقة** `{key, kind, supervisor, updatedAt, json}` — التقرير كلّه نصّاً في `json` لا
  خريطةً متشعّبة: مفاتيح النموذج (`score-1`, `notes-1`, …) لا تصلح أسماءَ حقولٍ في
  Firestore، والنصّ يُعفي من ذلك كلّه.
- **الجهاز هو الأصل:** الحفظ محلّيٌّ أوّلاً ثمّ يُرفع. فشل الرفع لا يمنع الحفظ ولا يُظهر
  خطأً — التقرير في مكانه.
- **الأحدث يغلب** بـ`updatedAt` (يُضاف إلى كلّ تقريرٍ عند حفظه). والقديم الذي لا يحمله
  يُختم عند أوّل مزامنة.
- **الحذف علامةٌ تُرفع** (`deleted:true`) لا وثيقةٌ تُمحى: بلا ذلك يرى الجهاز الآخر مفتاحاً
  عنده وليس في السحابة فيرفعه — فيعود المحذوف. ولذلك `allow delete: if false` في القواعد.
  وما عُدّل بعد الحذف (`updatedAt` أكبر) يُرفع ولا يُمحى.
- **ونصّ التقرير يبقى في وثيقة الحذف** (`json` مع `deleted`): الحذف بالخطأ يقع، وبلا النصّ
  لا رجعة فيه. `SupervisorCloud.deletedItems()` تسردها، و`restore(key)` تُرجع واحداً —
  يكتبه في الجهاز بتوقيتٍ جديدٍ ثمّ يرفعه، فتزول العلامة عن سائر الأجهزة عند مزامنتها.
  ولذلك يُنادى `remove(key, raw)` **بنصّ التقرير الملتقط قبل محوه محلّياً**.
- **ما يُرفع تقاريرُ الزيارات وحدها**: بادئتا `supervision_v6_visit_` و
  `supervision_v6_school_report_`. الهويّة ونسخة المعلمين والطواقم والأرشيف الرقميّ لا
  تغادر الجهاز.
- المزامنة عند فتح الموقع وعند الربط، وبزرّ «مزامنة الآن». وزرّ «إيقاف» يوقفها
  (`svf_cloud_off`) فتبقى التقارير محلّيّةً وحدها.
- بعد نزول تقارير أو حذفها يُطلق `svf-cloud-changed` فتُعاد قوائم الأرشيف.

**تنظيمٌ لا قفل — كقاعدة المعلمين:** القواعد تسمح بالقراءة بلا مصادقة، فمن عرف المسار
قرأ. السرّيّة الحقيقيّة هي المرحلة ٢ (Firebase Authentication) في `firestore.rules`.

**القواعد يجب أن تُنشر** في لوحة Firebase: `firestore.rules` في مستودع `teacher-data-collection`
ملفُّ مرجعٍ لا يُنشر تلقائياً (لا CLI على الجهاز).

## اسم المشرف في التقرير

- **الإشرافيّة:** `#visitorName` (توقيع الزائر) يُملأ من الهويّة إن كان فارغاً — ولا يُطمس
  ما كتبه المستخدم، فمن كتب اسم زميلٍ ناب عنه بقي ما كتب. ويُعاد ملؤه بعد «إعادة التعيين»
  لأنّها تمسح الحقول كلّها.
- **المدرسيّة:** لا حقل توقيعٍ في نموذجها، فيُحفظ `supervisor` مع التقرير ويظهر سطرُ «اسم
  الزائر» في ذيل المعاينة. والمعروض من التقرير المحفوظ أوّلاً لا من الهويّة، فتقريرٌ نزل من
  مشرفٍ آخر يُعرض باسم كاتبه.
- «المشرف العام» ليس اسم زائرٍ فلا يُكتب في تقرير.

**خللٌ أُصلح (2026-09-16):** `localStorage.setItem` في `savePermanentReport` كان بلا حارس،
فامتلاء تخزين المتصفّح يرميه: **التقرير يضيع ولا تظهر رسالة**، ويظنّ المشرف أنّه حُفظ.
(نظيرهُ المدرسيّ كان محروساً أصلاً.) والآن يُبلَّغ ويبقى النموذج معموراً. وأرشيفُ الرسوم
البيانيّة حارسُه مستقلّ، ففشلُه لا يُبطل حفظ التقرير نفسه.

**خللٌ أُصلح (2026-09-16):** `currentEditingKey` لم يكن معرَّفاً في أيّ ملف — يُنشأ ضمنياً
في `performReset` وحدها. فأوّلُ «حفظ التقرير» في صفحةٍ حديثة التحميل، بلا إعادة تعيينٍ
قبله، كان يرمي `ReferenceError` فلا يُحفظ التقرير **ولا يظهر خطأ**. صار معرَّفاً في
`js/state.js`.

## الإكمال التلقائيّ في النماذج (`js/autofill.js`)

يعمل بالربط وحده. بلا ربطٍ لا قوائم اقتراحٍ ولا تعبئة، والنماذج تُكتب يدوياً كما كانت.

- **الإشرافيّة:** `#teacherName` بقائمة اقتراحٍ بمعلّمي المشرف. عند الاختيار: الاسم يُوحَّد على
  تهجئة القاعدة، وتُملأ **المدرسة ورقم الملف** (إن كانا فارغين) و**الجنس** (يُطلق `change`
  فتُعاد صياغة الأوصاف). الاسم الملتبس يُعرض ولا يُختار عنه.
- **المدرسيّة:** `#schoolName` بقائمة مدارس، وزرّ **«استدعاء من قاعدة المعلمين»** في بطاقة
  الطاقم، واستدعاءٌ تلقائيٌّ عند ظهور البطاقة **والطاقم فارغ** (بعد الاستدعاء المحلّيّ في
  `loadSchoolRosterForSchool`، فالمحفوظ محلّياً أولى). التكرار يُمنع بمطابقة الاسم.
- **اسم المدير** يُعبَّأ حين تتّفق سجلّات المدرسة، ويُترك حين تختلف مع تنبيه — **٨٣ مدرسةً من
  ١٢٦ ذات سجلّين فأكثر تختلف فيها كتابته**. و«الفئة» (ذكور/إناث) ليست في القاعدة فتبقى فارغة.
- **لا طمس:** `svfFillIfEmpty` لا تكتب في حقلٍ فيه شيء.

**«بن» في الأسماء — خطرٌ عولج:** ٣٠٦ من ٥٠٢ سجلّاً تحمل «بن» بين الأسماء (للعمانيّين)،
والبوّابة قد لا تحملها. فالمطابقة في الموقع وفي السكربت تُسقط «بن/بنت» (`normName`)، والسكربت
منذ **v14.9** يبحث **بصيغةٍ بعد صيغة**: بلا «بن» ← كما كُتب ← الأوّل والأخير ← الأوّل
(`searchTerms` + `supRunSearch`/`supAwaitTeacherRow`). التوسيع في البحث لا في الاختيار:
الصفّ لا يُختار إلّا بمطابقة الاسم كاملاً، فلا يُكتب سجلٌّ رسميٌّ على معلّمٍ آخر.

**خلل أُصلح في `sync_supervisors.py` (2026-09-15):** كان يستبدل كتلة `SUPERVISORS` كاملةً،
وفي `data.html` داخلها `loadCodeOverrides`/`effHash`/`supByName` ← تشغيل «تحديث المشرفين»
كان سيحذفها فيتعطّل الدخول بالرموز المحدّثة. صار يستبدل القائمة وبصمة العام وحدهما.

## التقرير الشهري (`monthly.html` — منذ 2026-09-17)

صفحةٌ مستقلّةٌ تبني تقرير آخر الشهر بنموذج المشرف نفسه. **قراءةٌ فقط** من مصادر قائمة،
ولا تمسّ مفاتيح الموقع — مسوّداتها تحت `svf_monthly_*`.

| عمود التقرير | مصدره |
|---|---|
| الخطة الشهرية المعتمدة | خطة السير: `plan_<رقم المشرف>_<سنة>_<شهر0>` في مشروع `supervisor-plan`، ومفتاح اليوم `d_<اليوم>`. و`app_config/mandatory_events` (`سنة_شهر0_يوم`) تتقدّم عليها |
| الخطة المنفذة فعلياً | تقارير الزيارات المدرسيّة بتاريخ اليوم، باسمٍ مختصر (بلا «مدرسة» ولا نطاق صفوف، والخاصّة «(خ)») |
| ✓ | علامةٌ **لكلّ مدرسة** أكّد السكربت حفظها (`svf_sent_school_visits`) — لا لكلّ يوم |
| عدد الزيارات الإشرافية | «(1) أ. دينا (مرفق)» — و«مرفق» لمن لم يُؤكَّد حفظه في `svf_sent_visits`، ويُرفق ملفّه في الحزمة |
| المجموع | أيّام الخطة والمنفَّذ بلا أيّام «إجازة…»، و«11+(7) مرفق = 18»، والنسبة = المنفَّذ ÷ الخطة |

- **الأساليب الإشرافيّة** تُقترح: زيارةٌ صفّيّة ← «الحوار والمناقشة / زيارات صفية والمداولة»،
  ومدرسيّةٌ وحدها ← «الحوار والمناقشة»، وإلّا «-». وكلّ خليّةٍ تُعدَّل في شاشة المراجعة قبل الإخراج.
- **الأحمر** للإجازة التي خالفت الخطة (لا للإجازة الرسميّة المخطَّطة) ولأرقام «(1)».
- **`js/monthly-docx.js` لا يرسم تصميماً**: يستنسخ صفّ البيانات من نموذج المشرف بخصائصه،
  ويأخذ كلّ خليّةٍ من نموذجٍ بنوع نصّها (✓ من خليّة ✓، و«-» من خليّة نصّ لا من خليّة الرمز،
  وإلّا خرجت شرطةٌ غامقة). التظليل كتلاً من خمسة صفوف متّصلةً عبر الصفحتين، وسعةُ الصفحة
  الأولى = عدد صفوف جدولها في النموذج.
- **خليّة «الأسباب» سطران** في النموذج: الكتابة في الأوّل وحده تُبقي الثاني فيتكرّر النصّ.
- **النموذج يبقى في جهاز المشرف** (IndexedDB مستقلّة `MonthlyTemplateDB`): فيه اسمه وشعارات
  النموذج الرسميّ، فلا يُرفع مع الموقع.
- **الحزمة** `<البادئة> تقرير <الشهر>.zip` ← مجلّدٌ فيه التقرير و«الزيارات الاشرافية المرفقة»
  بملفّات Word من `getReportHTML` نفسها التي يصدّر بها الموقع.
- **كلٌّ يرى تقريره هو:** رقم خطة السير يُؤخذ من **هويّة المشرف** (`PLAN_NUMBERS` بأسماء
  قائمة `plan.supervisor-mct.com` وأرقامها، المطابقة بالاسم لا بالموضع) ويُقفَل الحقل. كان
  افتراضه **(6) للجميع**، فكلّ مشرفٍ يفتح الصفحة كان يسحب **خطة أسعد** ويبني عليها تقريره.
  وبادئة اسم الملف من اسمه، ومفاتيح المسوّدة لكلّ مشرفٍ على حدة (`svf_monthly_*_<id>`) —
  المتصفّح الواحد قد يتناوبه اثنان.
- **والزيارات زياراتُه:** التقرير الذي يحمل اسم **مشرفٍ آخر معروف** يُستبعد ويُقال عدده في
  شريط الحالة. وما لا اسم فيه أو فيه اسمٌ غير معروف (نيابةً عن زميل، أو تقريرٌ قديمٌ قبل
  الهويّة) يبقى لصاحب الجهاز: إسقاطه يُنقص تقريره الشهريّ، والنقص أفدح من زيادةٍ يراها ويحذفها.
- **الصفحة للمرتبط بقاعدة المعلمين وحده:** بطاقتها في الصفحة الرئيسيّة (`#monthlyCard`) مخفيّةٌ
  حتّى يُربط الرمز، وتُخفى فور فكّه (`svf-teachers-changed`). ومن فتح `monthly.html` برابطٍ مباشر
  بلا ربطٍ تُطوى أدواتها ويُقال له السبب — التقرير يُكتب باسم المشرف من القاعدة ويُبنى من زياراته
  لمعلّميه، فلا معنى له قبل الربط.
- **حدٌّ معروف:** تأكيدات البوّابة محفوظةٌ في متصفّح الجهاز الذي رُفع منه، وليست في المزامنة
  السحابيّة. فالتقرير يُنشأ من ذلك الجهاز، وإلّا نقصت علامات ✓ و«مرفق» زادت.
- اختباره `node tests/monthly.test.js` — وفحص ملء Word يحتاج نموذجاً: `SVF_MONTHLY_TEMPLATE=<ملف>`.

## توصيات الزيارة المدرسيّة (`js/recs.js` + `js/recs-ui.js` — منذ 2026-09-17)

منشئٌ فوق حقل التوصيات يبني البند بالاختيار، والحقل نفسه يبقى قابلاً للكتابة باليد.

- **`recs.js` محرّكٌ خالص** بلا DOM (يُصدَّر لـnode أيضاً): `buildText(list, {audience})` ←
  «نوصي ‹الجهة› بالآتي:» ثمّ «- » لكلّ بند ثمّ `CLOSING = 'والله الموفق.'`. النصّ مقيسٌ على
  مثالٍ حقيقيٍّ من المشرف ويُولَّد **حرفاً بحرف** كما كتبه (اختبارٌ يثبّته).
- **الأدوات**: العدد يظهر لما زاد على واحد فقط «مرمى لكرة اليد عدد (2)»، و«حسب الكشف المرفق»
  و«بالإضافة لباقي الأدوات في الكشف» خياران، وتخطيط الملعب يُلحق بالبند نفسه.
- **الضمير يتبع الجهة** (المعلم/المعلمة/المعلمين/المعلمات)، والجهة تُقترح من طاقم المدرسة
  (`audienceFromRoster`): معلّمةٌ واحدة ← «المعلمة»، ومختلطون ← «المعلمين».
- **المدّة** تُلحق بآخر البند («خلال أسبوعين عمل»)، ومنها يُحسب `dueDate` من تاريخ الزيارة —
  والمدّة التي لا أيّام لها (فصل دراسيّ، الزيارة القادمة) بلا تاريخ.
- **المتابعة** `carryOver`: ما لم يُنفَّذ يعود «التأكيد على …»، والمنفَّذ جزئياً «استكمال …».
- **البنية تُحفظ مع التقرير** (`recs`) لا النصّ وحده، فتُستعاد القائمة عند فتحه للتعديل،
  وتُبنى منها متابعة الزيارة التالية.
- **ما كُتب باليد لا يُطمس**: الإدراج في الحقل يستأذن إن لم يكن النصّ مولَّداً (`looksGenerated`).

## المواقف الصفّيّة وقاعدة المعلمين (`js/classroom.js` — منذ 2026-09-17)

يعمل بالربط وحده كالإكمال التلقائيّ؛ وبلا ربطٍ تبقى الحقول تُكتب باليد كما كانت، والزرّ
يعمل بما في الموقف الصفّيّ وحده.

- **اسم المعلّم يُردّ إلى تهجئة القاعدة** عند اختياره. ليس تجميلاً: وسم «حُفظت في البوابة»
  يطابق `teacher|date` حرفيّاً، فاختلاف رسم الاسم يُسقط الوسم.
- **الصفوف المقترحة صفوفه هو**: «5-12» تُفتح إلى ثمانية خيارات، و«9، 11» تبقى خيارين.
  والصفّ الخارج عنها **يُنبَّه عليه ولا يُمنع** — قد ينوب عن زميلٍ أو يتغيّر الجدول.
  والحصص (1)–(8) كما في البوّابة. وقائمة الأسماء نفسها من `autofill.js` فلا تُمسّ هنا.
- **الاسم المكرَّر في مدرستين** يُحسم بمدرسة الزيارة، وبلا مدرسةٍ لا يُختار عن الملتبس.
- **زرّ «أنشئ تقرير إشرافي»** في صفّ كلّ موقف: يفتح النموذج الإشرافيّ معبّأً (الاسم والمدرسة
  ورقم الملف من القاعدة وتاريخ الزيارة المدرسيّة والحصّة والصفّ وعنوان الدرس والمادّة والجنس)،
  وفوقه شريطٌ فيه «رجوع للزيارة المدرسية». وبعد الحفظ يصير الصفّ وسم **«له تقرير إشرافي»**.
  وأثرهُ يمتدّ إلى التقرير الشهريّ: موقفٌ بلا تقريرٍ إشرافيّ لا يُحتسب في «عدد الزيارات».
- **`performReset` لا تصلح هنا**: هي تمسح حقول الصفحة كلّها — ومنها نموذج الزيارة المدرسيّة
  المفتوح خلفنا — فالتصفير مقصورٌ على `#form-view`. وتقريرٌ إشرافيٌّ مفتوحٌ لم يُحفظ يُستأذن
  قبل استبداله، و`currentEditingKey` يُصفَّر فلا يُكتب التقرير الجديد فوق القديم.
- **ما جرّته القاعدة يبقى في التقرير ولا يُرسَل**: حمولة البوّابة تُقصَر على الحقول الخمسة
  (معلّم/صف/حصة/مهارة/تقييم) في `export.js` و`queue-export.js` — لا رقم ملفٍّ ولا جنس.
- **اسم المعلّم في رأي الزائر**: كان السطر بلا اسمٍ عمداً («سجلٌّ عن المدرسة لا عن الأشخاص»)
  وطلب المشرف إظهاره — `classroomVisitLine()` تكتبه مسبوقاً بـ«المعلم/المعلمة» من **جنسه**
  (من بيانات الموقف أو من الطاقم)، ومن لا يُعرف جنسه يُكتب اسمه مجرّداً فلا يُذكَّر معلّمة.
  والموضعان (الهدف المدموج والبند المستقلّ) يكتبان من الدالّة نفسها.

## خطّ سير اليوم: «قادم من» و«متجه إلى» (`js/route.js` — منذ 2026-09-21)

من كان له أكثر من زيارةٍ مدرسيّةٍ في اليوم يكتب في **آخر خانة الأهداف** بالبوّابة سطراً
«#قادم من مدرسة …» و/أو «#متجه إلى مدرسة …». في نموذج الزيارة المدرسيّة، **أسفل قائمة الأهداف
مباشرةً**، خانتان (`#schoolCameFrom` / `#schoolGoingTo`) داخل `reportForm` — فيصفّرهما `reset()`
في «تقرير جديد» — ومعهما اقتراحٌ يُنقر: مدارس زياراتٍ أخرى **محفوظةٍ في اليوم نفسه** (زرّا
«قادم منها» و«متجه إليها»).

- **حقلان مستقلّان في التقرير** (`cameFrom` / `goingTo`) — **لا يدخلان `report.objectives`**:
  `restoreSchoolObjectives` تنبّه على كلّ هدفٍ محفوظٍ لا تجده في قائمة النوع (فكان سيُنبَّه في
  كلّ فتح)، ولا يصحّ أن يُحتسبا هدفاً في التحقّق قبل الرفع.
- **يُلحقان بالمصفوفة المرسَلة وحدها**: `SchoolRoute.routeLines(cameFrom, goingTo)` ← السكربت يدمج
  المصفوفة بسطرٍ لكلّ عنصر (`objectives.join('
')` في «موضوع الزيارة»)، فيصلان بعد الأهداف دون
  تغييرٍ في مسار التعبئة. **لا بدّ من ثلاثة مواضع**:
  1. `js/export.js` (`exportSchoolVisitToMoe`) — التصدير المفرد من الموقع؛
  2. `js/queue-export.js` (`schoolToPortal`) — الطابور من الأرشيف؛
  3. **`school-visits-automation.user.js` (`doExport`)** — السكربت **يعترض زرّ التصدير في الموقع**
     (`patchExportBtn`: `preventDefault` + `stopPropagation`) ويقرأ النموذج بنفسه، فمن ثبّته لا
     يمرّ زرّه بـ`export.js` أصلاً. لذلك للسكربت **نسخته المستقلّة** من `svfRouteName/svfRouteLines`
     (v15.2)، و`tests/route.test.js` يقارنها بنسخة الموقع على المدخلات نفسها — **عدّلهما معاً**.
- **الصياغة**: تُسبَق الجهة بـ«مدرسة» إلا إن بدأت بـ(مدرسة/مدارس/معهد/مركز/كلية/روضة/إدارة/دائرة/مديرية)،
  ويُحذف نطاق الصفوف «(1-12)» من اسمها (يلحق الاسمَ في الموقع ولا يُكتب في السطر). والسطر يبدأ بـ«#»
  فلا يُحتسب هدفاً: `schoolValidate` تُسقطه، فزيارةٌ بلا أهدافٍ وبخطّ سيرٍ وحده تبقى ناقصة.
- **يظهر أيضاً** في معاينة التقرير (بعد القائمة المرقَّمة، غير مرقَّم، والنصّ مهروب)، وفي «نسخ الأهداف»،
  وفي نافذة تأكيد التصدير (صفٌّ مستقلّ، فلا يُقصّ خلف «...»).
- **اقتراح مدارس اليوم اقتراحٌ لا تعبئة**: ترتيب الزيارات في اليوم غير معروف (الوصول والانصراف
  يبقيان غالباً 08:00/12:00) فلا يُخمَّن أيّها سابق.
- **الخانتان تتبعان قائمة المدارس عند الربط** (`svfBindList`) وتُفكّان عند إلغائه، كخانة المدرسة.

**ترقيم الأهداف عند التصدير (أُصلح 2026-09-21):** كانت البوّابة تتلقّى «موضوع الزيارة» كتلة
نصٍّ متلاصقة بلا ترقيم — رقم القائمة الكاملة يُنزَع في `export.js`/`queue-export.js`/السكربت
لأنّه لا يطابق المحدَّد (قد يكون ١، ٣، ٥ من قائمةٍ من سبعة)، ولم يكن يُعاد ترقيمها بعده.
الآن تُرقَّم تسلسليّاً ١، ٢، ٣، … **بنفس ترقيم رأي الزائر** (كلاهما من الأهداف المحدَّدة
بالترتيب نفسه، فالبند رقم ٢ في الأهداف هو البند رقم ٢ في رأي الزائر). خطّ سير اليوم يبقى
بلا ترقيم كما هو (سطرٌ يبدأ بـ`#`، وليس هدفاً). **الترقيم في المواضع الثلاثة نفسها التي
تحمل نسخة خطّ السير** (`js/export.js`، `js/queue-export.js`، والسكربت v15.3) — تعديلٌ رابع
منها إن أُضيف مسارٌ جديد.

## ثبات الأهداف المحدَّدة بين الحفظ والفتح (أُصلح 2026-09-17)

**خللٌ كان يُفقد التحديد:** الهدف يُحفظ **بنصّه بعد تطبيق مفتاح الجنس**
(`[معلم/معلمة/معلمي/معلمات]` ← `applyGenderFilter`)، والاستعادة كانت تطابق النصّ حرفاً
بحرف — **والمفتاح نفسه لا يُحفظ**. فمن حدّد أهدافه بصيغة «معلمات» ثمّ فتح التقرير للتعديل
والمفتاح عاد إلى «معلم» وجد أهدافه **بلا تحديد**، وتبقى وحدها الأهدافُ التي لا صيغ جنسٍ فيها
— وهو ما يبدو «تغيّرت» لا «اختفت».

- **`genderMode` يُحفظ مع التقرير** ويُضبط **قبل** `renderSchoolObjectives` (نصّ الهدف يُبنى عليه).
- **التقارير القديمة** (بلا مفتاح) يُستنتج مفتاحها من أهدافها: أكثر الصيغ مطابقةً لما حُفظ.
- **المطابقة بعدها بالموضع** لا بالنصّ وحده: الهدف يُطابَق بموضعه في قائمة النوع مهما اختلفت صيغته.
- **ما لم يُطابَق يُقال** بتنبيه «(ن) من أهداف التقرير لم تعد في قائمة هذا النوع» — الهدف الذي
  حُذف أو غُيّر نصّه في الإعدادات بعد الحفظ كان يسقط صامتاً.
- **ملاحظات الأهداف تُحفظ مع التقرير** (`objectiveNotes`) وتُستعاد؛ وكانت في الذاكرة وحدها
  فتضيع عند الفتح، **وتُورَّث من التقرير المفتوح قبله** فتُنسب ملاحظةُ زيارةٍ إلى زيارةٍ أخرى.
- المسارَان (الفتح للتعديل، والرجوع من المعاينة) يستعيدان بدالّةٍ واحدة `restoreSchoolObjectives`.
- اختباره `node tests/objectives.test.js` — يشغّل `js/school.js` نفسه على DOM مصغّر.

## تعديل نصوص الأهداف (منذ 2026-09-25)

من أراد تعديلاً خفيفاً في صياغة هدفٍ جاهز (لا كتابة هدفٍ جديد) يضغط على نصّه في
نموذج الزيارة المدرسيّة ويعدّله مباشرةً، مع بقاء مربّع تحديد الهدف يعمل بذاته.

- **الرقم الثابت خارج التعديل، والجسم وحده داخله.** كلّ هدفٍ نصّه `رقم- جسم` —
  `renderSchoolObjectives` تفصلهما بـ`resolved.match(/^([\d٠-٩]+\s*[-–]\s*)([\s\S]*)$/)`
  إلى `prefix` (يُعرض في `<span>` ثابت) و`body` (فى زرٍّ `.obj-text` قابل للنقر). تعديل
  الجسم لا يُحرّك الرقم، وترقيم التصدير التسلسليّ (القسم أعلاه) لا يتأثّر.
- **النصّ خرج من داخل `<label for="obj-…">`.** كانت الاستمارة القديمة تغلّف النصّ كلّه
  بوسم `label` لمربّع التحديد، فالنقر على أيّ كلمةٍ منه كان يُبدّل التحديد لا يفتح تعديلاً؛
  هذا هو ما كان يمنع الميزة أصلاً. الآن `label`ه مقصورٌ على مربّع التحديد نفسه (`sr-only`
  لإتاحته لقارئ الشاشة)، والنصّ عنصرٌ مستقلٌّ بجانبه.
- **صفّان متبادَلان لا حقلٌ دائم:** `.obj-display` (نصٌّ + زرّ) و`.obj-editing`
  (نصٌّ + `<input>`) — يظهر أحدهما ويُخفى الآخر بـ`.hidden`. النقر على `.obj-text` يفتح
  حقل التعديل ويُركّز عليه ويُحدّد نصّه، ومغادرته (بالنقر خارجاً، أو Enter، أو Escape)
  تُغلقه وتُعيد صفّ العرض.
- **`objectiveEdits` بيانا التعديل** (`js/state.js`): كائنٌ مفتاحه موضع الهدف في قائمة
  نوعه وقيمته نصّه الكامل بعد التعديل (بالرقم الثابت أوّله). غيابه يعني: النصّ الافتراضيّ
  كما في الإعدادات. يُحفظ مع التقرير (`saveSchoolReport`) ويُستعاد في **المسارين نفسيهما**
  اللذين يستعيدان `objectiveNotes` (الفتح للتعديل، والرجوع من المعاينة بلا تقريرٍ محفوظ)
  — **قبل** `renderSchoolObjectives`، إذ نصّ الهدف يُبنى عليه. ويُصفَّر مع بقيّة حالة
  النموذج في «تقرير جديد» (`js/init.js`).
  ويُحفظ عبر `collectObjectiveEdits()` التي تقرأ **من الفورم نفسه لحظة الحفظ** — من
  `.obj-edit-input` إن كان حقل التعديل مفتوحاً حين يُضغط «حفظ»، لا من `objectiveEdits`
  وحدها، فتعديلٌ لم يُغادَر حقلُه بعد لا يضيع. وحمايةً إضافيّة: `saveSchoolReport` تُغادر
  (`blur()`) أيّ حقل تعديلٍ مفتوحٍ قسراً قبل قراءة الفورم، فلا يبقى مربّع تحديد الهدف
  حاملاً نصّه القديم بينما التعديل الجديد لا يظهر إلّا بعد إعادة فتح التقرير.
  **إعادة الضبط بشارة «معدَّل»:** كلّ هدفٍ فيه تعديلٌ يحمل شارةً صغيرة، والضغط عليها
  يمحو `objectiveEdits[index]` ويعيد النصّ الافتراضيّ دون أن يمسّ تحديده.
- **تغيير مفتاح الجنس لا يفقد التعديل:** خطوة «الجمع قبل المحو» في `renderSchoolObjectives`
  (التي تحفظ حالة التحديد/الملاحظات قبل إعادة الرسم عند تبديل النوع) تحفظ الآن
  `objectiveEdits[i]` أيضاً بالمقارنة مع `item.dataset.default`، فتعديلٌ كُتب بصيغة «معلمة»
  يبقى معروضاً بعد التحويل إلى «معلم» (والجسم المُعدَّل نفسه، لا الرقم، هو ما يُقارَن).
- **خللٌ حقيقيٌّ كشفه اختبار CDP حيّ لا الوحدات:** معالج Escape الأوّل كان يُخفي
  `.obj-editing` مباشرةً (`classList.add('hidden')`) دون مغادرة الحقل أوّلاً. وإخفاء
  عنصرٍ عليه التركيز بـ`display:none` (ما تفعله `.hidden` في Tailwind) **يُطلق `blur`
  تلقائيّاً** حسب معيار المتصفّح نفسه — فكان هذا الإخفاء يُشغّل `commit()` بنصّ الحقل
  **قبل** إلغائه فعليّاً، أي يحفظ بالضبط ما ضغط المستخدم Escape ليتراجع عنه. لم يظهر هذا
  في اختبارات الوحدة (DOM مصغّرٌ لا يُطلق `blur` تلقائيّاً عند الإخفاء)، وإنّما في اختبار
  Chrome headless حقيقيّ بأحداث CDP `Input.*` (النقر/الكتابة/المفاتيح الفعليّة — الأحداث
  الصناعيّة في Chrome headless لا تُطلق `blur`/`focusout` بصورةٍ موثوقة). **الإصلاح:**
  علَمٌ `cancelling`؛ Escape يرفعه ثمّ يستدعي `editInput.blur()` صراحةً (فيمرّ بمسار
  `commit()` نفسه الذي يمرّ به Enter والنقر خارجاً)، و`commit()` يتحقّق من العلَم أوّل
  شيءٍ ويمسحه دون حفظٍ إن كان مرفوعاً.
  والجسم الفارغ عند المغادرة (بلا Escape) يُلغى صامتاً أيضاً — لا هدف بلا نصّ.
- اختباره `node tests/objectives.test.js` — امتدّ ليغطّي فصل الرقم/الجسم، ودالّة
  `collectObjectiveEdits`، وبنية الربط الحيّ (٣٩ فحصاً، منها ما يصطاد رجوع خلل `cancelling`
  تحديداً). واختبار Chrome headless الحيّ (خارج المستودع) غطّى الدورة كاملةً: نقرٌ لا يبدّل
  التحديد، حفظٌ وإعادة فتحٍ يحفظان التعديل والتحديد معاً، Escape يُلغي بلا حفظ، رأي الزائر
  والتصدير يعكسان النصّ المُعدَّل بترقيمه الصحيح.

## طاقم المدرسة في رأي الزائر (الزيارات المدرسية)

قسمٌ في نموذج الزيارة المدرسية يظهر **للزيارات الاستطلاعيّة وحدها**
(`updateRosterVisibility` ← `isExploratoryType`): اسم المدير وجنسه، ومعلمو الرياضة
باسمٍ وجنسٍ ونصابٍ وصفوفٍ وفئة. يُحفظ مع التقرير **وباسم المدرسة** في
`supervision_v6_school_roster_<اسم المدرسة>`، فيُستدعى عند كتابة الاسم نفسه لاحقاً
(ولا يُستدعى إلّا والقائمة فارغة، فلا يطمس إدخالاً جديداً).

عند التوليد يدخل الطاقم في بندين:

- **بند المقابلة/الالتقاء** (`buildMeetSentence`): اسم المدير بعد «المدرسة»، وأسماء
  المعلمين بين قوسين بعد كلمتهم. **لقب المدير وكلمة المعلّمين يُشتقّان من البيانات لا من
  مفتاح الجنس والعدد** — فالمديرة قد يكون طاقمها معلمين، ومفتاحٌ واحدٌ للنصّ كلّه لا يصفهما.
- **بند النصاب** (`teacherLoadLines`): سطرٌ لكلّ معلم، والفعل «يدرس/تدرس» من جنسه.

`wrapNumbersInOpinion` تُغلّف الأرقام بأقواس لاستقامة عرضها في نصٍّ من اليمين لليسار،
وتترك ما بين قوسين كتلةً واحدة (وإلّا مُزّق «(5-12)» إلى «(5-(1)2)»)، ولا تمسّ ترقيم البنود.

## معاينة الزيارة المدرسيّة والرجوع منها

للمعاينة مدخلان، وزرّ «رجوع للنموذج» يختلف حكمه بينهما — و`generateSchoolPreview(report, fromForm)`
هو ما يميّزهما:

- **من زرّ «معاينة»** (`fromForm = true`): النموذج معمورٌ بما كتبه المشرف، إنّما أُخفي.
  فالرجوع يُظهره كما هو ولا يمسّه.
- **من سجلّ التقارير** (`fromForm` غائب): النموذج فارغ، فالرجوع يُحمّل التقرير المحفوظ
  (`editSchoolReport`).

**خللٌ أُصلح (2026-09-16):** كان الرجوع يُعيد التحميل من التخزين كلّما كان للتقرير مفتاح —
أي في كلّ تقريرٍ سبق حفظه. فمن ولّد **رأي الزائر** ثمّ عاينه ثمّ رجع، وجده قد عاد إلى آخر
نصٍّ محفوظ (وهو فارغٌ غالباً). وأسوأ منه الفرع الآخر: حمولةُ المعاينة لا تحمل `teachers` ولا
`principal` ولا وقتَي الوصول والانصراف، فكان الرجوع في التقرير الجديد **يمسح طاقم المدرسة
واسم المدير والوقتين**. المعاينة الآن لا تكتب في النموذج شيئاً.

## رفع دفعةٍ من الأرشيف (منذ v14.3)

في تبويب الأرشيف: مربّع اختيارٍ على كلّ بطاقة، وشريطٌ فيه «تحديد الكل» (المعروض بعد
التصفية فقط) و«إلغاء التحديد» (يشمل المخفيّ) و«رفع المحدد إلى البوابة».

- `svfSendSelected()` تبني الطابور من المفاتيح المحدَّدة، **تستبعد الناقص وتذكر سببه
  بالاسم**، وتُبقيه محدَّداً ليُصلَح، ثمّ تفتح البوّابة بـ`#svfs=` وفيه `{kind, visits[]}`.
- الوسوم على البطاقة: **«سبق رفعها»** (`svf_queued_visits` — رُفعت من هنا، لا تعني أنّها
  حُفظت) و**«حُفظت في البوابة»** (`svf_sent_visits` — تأكيدٌ حقيقيّ من البوّابة).
- `window.open` داخل ضغطة المستخدم نفسها؛ التأجيل يُفقدها التصريح فيحجبها المتصفّح.

## طابور الزيارات المدرسيّة (منذ v14.5)

نظيرُ طابور الإشرافيّة في «سجل التقارير المدرسية»، بمسارٍ مستقلٍّ تماماً:

| | الإشرافيّة | المدرسيّة |
|---|---|---|
| الرابط | `#svfs=` ← SupervisionVisits | `#svf=` ← VariousRecords/SchoolVisits |
| الحمولة | `{kind:'supervision', visits}` | `{kind:'school', visits}` |
| التحديد | `svfSelected` | `svfSchoolSelected` |
| «سبق رفعها» | `svf_queued_visits` | `svf_queued_school` |
| «حُفظت» | `svf_sent_visits` — `المعلّم\|التاريخ` | `svf_sent_school_visits` — `المدرسة\|التاريخ` |
| طابور السكربت | `svf_queue` / `_i` / `svf_queue_hold` | `svf_school_queue` / `_i` / `svf_school_hold` |

- نوع البوّابة من **اسم** نوع الزيارة: استطلاعية ٢، إشرافية ١، أخرى ٣.
- الاستبعاد: بلا مدرسة، أو تاريخ، أو أهداف، أو (رأي زائر وتوصيات معاً).
- وقتا الوصول والانصراف صارا يُحفظان في التقرير؛ التقارير الأقدم تُرسَل بـ08:00/12:00.
- في السكربت: `schoolQueueGate()` أوّل ما في `runAutoFull` و`runFillOnly`، والعلامة
  تُوضع لحظة ظهور نموذج الإضافة، والتقدّم في `finally` بعد حفظٍ مؤكَّد — ثمّ تبدأ
  الزيارة التالية من اختيار المدرسة بعد تصفير مراحل الطيّار.

**تأكيدٌ يدويٌّ لوسم «حُفظت» (منذ 2026-09-26):** الكشف التلقائيّ يعتمد على
سكربت تامبر مانكي على **الجهاز نفسه** (يكتب `svf_saved_visits`/`svf_saved_school_visits`
بتخزينه الخاصّ `GM_setValue`، ويُضخّ إلى `localStorage` الموقع عند العودة إلى
تبويبه) — فمن فتح الأرشيف من جهازٍ آخر بعد أن تصله التقارير عبر النسخة
السحابيّة (المزامنة لا تنقل هذا الوسم، فهو محليٌّ بتصميمه)، أو لم يكن السكربت
مثبَّتاً وقت الحفظ، يرى الزيارة بلا وسم رغم أنّها محفوظةٌ فعلاً. زرٌّ صغيرٌ
«✓ تأكيد الحفظ يدويّاً» يظهر بجانب كلّ زيارةٍ غير موسومةٍ بـ«حُفظت» في كلا
الأرشيفين، ويكتب المفتاح نفسه الذي يكتبه الكشف التلقائيّ
(`window.svfMarkSent`/`window.svfSchoolMarkSent` في `js/queue-export.js`) —
فلا فرق بين الوسمين بعدها. اختباره `node tests/manual-confirm.test.js`.

## حفظ الزيارة المدرسيّة — ما كشفه أوّل تشغيلٍ حقيقيٍّ للطابور (v14.8)

من سجلّ 2026-09-14: ضغط `#btnAdd` ← بعد ثانيتين ظهر في عناصر الرسائل النصّ **«123»** ←
بعد خمس ثوانٍ **أُعيد تحميل الصفحة كاملةً** إلى القائمة. السكربت عدّ «123» رفضاً فتوقّف،
والبوّابة كانت قد حفظت.

- **البوّابة تحفظ عبر إعادة تحميل الصفحة**، فيموت السكربت وهو ينتظر النتيجة. لذا تُكتب
  `svf_save_pending` (وقت، مسار، مدرسة، تاريخ) قبل الضغط، و`resolvePendingSaveAfterReload`
  أوّلَ ما يجري عند التحميل تحكم: **حُفظت** إن عادت الصفحة نفسها إلى القائمة، والنموذج
  مغلق، خلال دقيقة، للزيارة نفسها، بلا رسالة رفض. أيّ شرطٍ يختلّ ← لا حكم (والطابور يسأل).
- الرسائل تُحسب **إن كانت جديدةً بعد الضغط وفيها كلمات** — الأرقام المجرّدة والنصوص
  الموجودة قبل الضغط تُهمل. ثمّ تُصنَّف: النفي/الخطأ أوّلاً، ثمّ «تم الحفظ/بنجاح»، والمجهول رفض.
- أزرار اللوحة تقرأ `visitData` لحظة الضغط (كانت تحمل الزيارة الأولى ← تكرار).
- السجلّ يُحفظ في `svf_school_log` بالجلسة فيبقى عبر إعادة التحميل، وزرّ «نسخ السجل».

## اختيار المدرسة وأنظمة التعليم (منذ v14.6)

في صفحة الزيارات المدرسيّة لا تظهر المدرسة في قائمة المدارس إلّا تحت **نظام تعليمها**
(عام، أساسي، دولي، ثنائي اللغة، ثنائي اللغة خاص، …). المدارس الخاصّة الدوليّة تحت «دولي».

- `findAndSelectSchool` يبحث في النظام الحالي، ثمّ يحوّل النظام ويعيد البحث بترتيب
  `eduCandidates`: `data.eduSystem` ← ذاكرة `svf_school_edu_map` ← إيحاء الاسم
  (دولية/ثنائية/خاصة) ← الشائع ← **كلّ** ما بقي في القائمة.
- تغيير النظام قد يُعيد الصفحة كاملةً: `svf_edu_search` في sessionStorage تحفظ ما جُرِّب
  قبل كلّ تحويل، وفرع التهيئة يستأنف البحث إن وجدها.
- **مطابقة النظام تامّةٌ بعد التسوية** — كانت `includes('خاص')` تختار «تربية خاصة بصري».
- **مطابقة المدرسة بلا تخمين** (`matchSchool`): تامّة، أو احتواءٌ فريد، أو كلّ الكلمات
  المميِّزة في مدرسةٍ واحدة. التعدّد يُعرض على المستخدم ولا يُختار منه ولا يُغيَّر النظام.
  **المطابقة القديمة بأوّل كلمةٍ حُذفت**: «مدرسة …» كانت تطابق أيّ مدرسة ← سجلٌّ رسميٌّ
  على مدرسةٍ خاطئة، وتجربة كلّ الأنظمة كانت ستضاعف احتماله.
- قائمة الأنظمة تُعرف بخياراتها (اثنان على الأقلّ من أسماء الأنظمة المعروفة)، لا بكلمة
  «أساسي» التي تحملها أسماء مدارس كثيرة.
- **الأرقام في اسم المدرسة تُعامَل وحدها (v15.1):** الموقع يكتب نطاق الصفوف «(1-12)»
  والبوّابة كثيراً ما تُسقطه. كان «12» كلمةً مميِّزةً لا بدّ منها، فلم تُطابَق «الأجيال العصرية
  الدولية (1-12)» — وهي في البوّابة «الأجيال العصرية الدولية الخاصة» تحت «ثنائي اللغة» — في
  أيّ نظام. الآن **الكلمات تُشترط والأرقام لا تُشترط لكنّها تُقصي**: مدرسةٌ في البوّابة تحمل
  أرقاماً ليست في اسم الموقع (نطاقٌ آخر أو فرع) ليست هي. ورقمٌ هو جزءٌ من الاسم («18 نوفمبر»)
  يبقى يُطابِق.

## طيّ اللوحتين (منذ v15.0)

اللوحتان تبدآن **مطويّتين** — شريطاً بعرض ١٨٠–٢٢٠ بكسل — فلا تحجبان الاستمارة. لوحة
الزيارات المدرسيّة كانت تُفتح مبسوطةً بـ٣٤٢×٨١٦ بكسل: أطول من شاشة ٧٦٨.

- الضغط على **الشريط كلّه** يطوي ويبسط، والاختيار يبقى ما دام التبويب مفتوحاً
  (`svf_v7_folded` / `svfs_folded` في sessionStorage) — البوّابة تُعيد تحميل الصفحة مراراً.
- **السحب ليس ضغطة:** `panel.dataset.dragged` يُعلَّم إن تحرّك الفأر أكثر من ٤ بكسل، فنهايةُ
  السحب لا تبسطها. (في الإشرافيّة كان نقلُ اللوحة المطويّة يبسطها.)
- **تُبسط وحدها** عند سطر `error` في السجلّ، و**عند مهلة الحفظ** — وهذا شرط سلامة لا راحة:
  زرّ «إلغاء الحفظ» يُرسم **داخل** جسم اللوحة، فمطويّةً يمضي الحفظ التلقائيّ بلا فرصة إلغاء.
  لوحة الإشرافيّة كانت مطويّةً افتراضاً ومهلتُها لا تبسطها — أي أنّ الإلغاء كان مخفيّاً فيها.

## وضع الطابور (منذ v14.2)

الطابور يعمل **بالحفظ التلقائيّ فقط**، وهذا ليس تفضيلاً بل شرطُ سلامة: `queueAdvance()`
يُستدعى من فرع الحفظ التلقائيّ في `supSave` وحده، فالحفظ اليدويّ لا يُعلِم السكربت،
فيبقى المؤشّر على زيارةٍ حُفظت وتُعبَّأ **مرّةً ثانية** ← سجلٌّ رسميٌّ مكرّر.

- `queueGate()` تُستدعى أوّلَ ما في `supFill`، فتمنع التعبئة والطيّارَ الآلي معاً إن
  كان الطابور قائماً والحفظ التلقائيّ مطفأً، وتشرح السبب في السجلّ.
- `svf_queue_hold` = فهرس زيارةٍ بلغت صفحة التقييم ولم يتأكّد حفظها (فشلٌ أو إلغاءٌ أو
  انتهاء مهلة). عند التعبئة التالية يُسأل المستخدم: أمحفوظةٌ فنتخطّاها، أم لا فنعيدها.
  `queueAdvance()` يمسح العلامة عند الحفظ المؤكَّد.
- كشفُ العودة لصفحة القائمة وحده **لا يصلح** بديلاً: الإلغاء يعيد إليها أيضاً فتُتخطّى
  زيارةٌ لم تُحفظ.
- الحفظ التلقائيّ يبقى **مهلةَ ٦ ثوانٍ قابلةً للإلغاء** (`SAVE_GRACE_MS`) ولا يضغط
  «حفظ» إلّا بعد التحقّق من أنّ كلّ حقلٍ يحمل ما كُتب فيه.

## قواعد لا يُتنازل عنها

- **لا حفظ تلقائيّ قبل اكتمال التحقّق.** السجلّ رسميّ باسم المستخدم، وزيارةٌ ناقصةٌ
  أسوأ من زيارةٍ غير محفوظة. (`autoSaveOn()` افتراضها `true` في الشيفرة منذ 11b0943،
  خلافاً لما كان مكتوباً هنا؛ والزرّ في اللوحة يقلبها ويحفظ الاختيار.)
- **لا توزيع نصوصٍ بالترتيب عند فشل المطابقة بالاسم.** يُترك الحقل ويُبلَّغ المستخدم.
- **لا تخمين لسلّم التقييم.** إن لم تُطابَق القيم، أبلغ ولا تكتب.
- إن كانت قوائم التقييم بلا خيارات سوى «غير مقيم» فهذا عطلٌ في البوّابة
  (استمارة قيد التحديث) — يتوقّف السكربت ولا يحفظ.

## أدوات التشخيص المدمجة

- زرّ **«تشخيص الصفحة»** — يسرد كلّ الحقول والشبكات في المستند وإطاراته، ويطبع
  سلّم التقييم بقيمه.
- **المسجّل** — يعترض `__doPostBack` ويطبع وسائطه، ويسجّل كلّ نقرة. هو ما كشف
  `DisplayInfo`. الأسطر معلّمة بـ 📼.
- **المحاكاة** في `sim-moe/` — تعيد قيود البوّابة الأربعة، فتُختبر التعديلات في
  ثوانٍ بلا سجلٍّ رسميّ. السكربت يعامل مسار `sim-moe` معاملة البوّابة.

## الحالة الآن

المسار يعمل من الضغطة الأولى حتّى مرحلة التقييم. المتبقّي:

1. **تبويب المهارات التخصصيّة** (٢١ مهارة) لا يقابله شيء في الموقع — يحتاج حقولاً جديدة.
2. **اختبار انحدار** على المحاكاة قبل كلّ إصدار، ومعه `node tests/queue-and-config.test.js`
   و`node tests/identity.test.js` و`node tests/cloud.test.js` و`node tests/monthly.test.js`
   و`node tests/recs.test.js` و`node tests/classroom.test.js` و`node tests/objectives.test.js` و`node tests/route.test.js`.
   ملف المحاكاة نفسه لم يُرفع بعد.
3. **مطابقة الدرجة لها ملاذٌ أخيرٌ بالموضع** (`opts[Number(v)]` في تعبئة البنود) — تخمينٌ
   يخالف قاعدة «لا تخمين لسلّم التقييم». يُراجَع حين يُطبع سلّم البوّابة الحقيقيّ من التشخيص.
4. **وسم «حُفظت» يطابق بالاسم والتاريخ حرفيّاً** (`teacher|date`). تعديل اسم المعلّم في
   الأرشيف بعد الحفظ يُسقط الوسم؛ لا تسوية عربيّة هنا بعد.
5. **أمان:** بيانات دخول البوّابة كانت بنصٍّ صريحٍ في ملف مهارة `supervisory-report` —
   يجب حذفها وتغيير كلمة المرور. السكربت يخزّنها في تامبر مانكي لا في الشيفرة.

## أخطاء متكرّرة يجب تفاديها

- **التجربة على نسخةٍ قديمة.** حدث ثلاث مرّات وأضاع دورات كاملة. تحقّق من رقم
  النسخة في تامبر مانكي قبل كلّ اختبار.
- **سجلّ اللوحة يعرض الأحدث أوّلاً** — اقرأه من الأسفل للأعلى عند تتبّع التسلسل.
- **عدم التمييز بين عطل السكربت وعطل البوّابة.** ضاع وقتٌ طويلٌ في مطاردة
  فشلٍ مصدره أنّ الاستمارة في البوّابة نفسها كانت بلا خيارات تقييم.
